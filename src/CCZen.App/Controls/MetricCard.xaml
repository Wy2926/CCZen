<UserControl x:Class="CCZen.App.Controls.MetricCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            CornerRadius="6" Padding="14,12" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header: icon + title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock x:Name="IconText" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                <TextBlock x:Name="TitleText" FontSize="12" FontFamily="Microsoft YaHei"
                           Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Value area -->
            <StackPanel Grid.Row="1" Margin="0,0,0,4">
                <TextBlock x:Name="ValueText" FontSize="22" FontFamily="Cascadia Code,Consolas"
                           Foreground="{DynamicResource PrimaryTextBrush}" />
                <TextBlock x:Name="SubValueText" FontSize="11" FontFamily="Microsoft YaHei"
                           Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0" />
            </StackPanel>

            <!-- Optional progress bar -->
            <Grid Grid.Row="2" x:Name="ProgressArea" Visibility="Collapsed" Margin="0,4,0,0">
                <ProgressBar x:Name="ProgressBar" Height="4" Maximum="100" />
                <TextBlock x:Name="ProgressText" FontSize="10" FontFamily="Cascadia Code,Consolas"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Right" Margin="0,6,0,0" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
