<UserControl x:Class="CCZen.App.Controls.MessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed">
    <!-- Full-screen overlay -->
    <Grid>
        <!-- Dim background -->
        <Border Background="#80000000" />

        <!-- Dialog card -->
        <Border Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                CornerRadius="8" Padding="24,20"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                MinWidth="360" MaxWidth="480">
            <StackPanel>
                <!-- Title -->
                <TextBlock x:Name="TitleText" FontSize="15" FontWeight="SemiBold"
                           FontFamily="Microsoft YaHei"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,12" />

                <!-- Message -->
                <TextBlock x:Name="MessageText" FontSize="13"
                           FontFamily="Microsoft YaHei"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           TextWrapping="Wrap" Margin="0,0,0,20" />

                <!-- Buttons -->
                <StackPanel x:Name="ButtonPanel" Orientation="Horizontal"
                            HorizontalAlignment="Right" Margin="0,4,0,0">
                    <Button x:Name="CancelBtn" Content="取消" Margin="0,0,10,0"
                            Click="OnCancelClick" Visibility="Collapsed" />
                    <Button x:Name="ConfirmBtn" Content="确定"
                            Style="{StaticResource AccentButton}"
                            Click="OnConfirmClick" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
