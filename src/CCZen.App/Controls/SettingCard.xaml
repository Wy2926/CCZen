<UserControl x:Class="CCZen.App.Controls.SettingCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            CornerRadius="6" Padding="16,12" Margin="0,0,0,8" SnapsToDevicePixels="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="IconText" FontSize="18" Margin="0,0,14,0" VerticalAlignment="Center"
                       Foreground="{DynamicResource AccentBrush}" />

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="HeaderText" FontSize="13" FontFamily="Microsoft YaHei"
                           Foreground="{DynamicResource PrimaryTextBrush}" />
                <TextBlock x:Name="DescriptionText" FontSize="12" FontFamily="Microsoft YaHei"
                           Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"
                           Margin="0,2,0,0" />
            </StackPanel>

            <ContentPresenter Grid.Column="2" VerticalAlignment="Center" Margin="16,0,0,0"
                              Content="{Binding ActionContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Grid>
    </Border>
</UserControl>
