<Page
    x:Class="CCZen.App.Views.LargeFilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:CCZen.App.Models">

    <Grid Padding="{StaticResource PagePadding}" MaxWidth="1100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="大文件搜索" Style="{StaticResource PageTitleStyle}" />
            <TextBlock
                Text="基于 MFT/USN 的内存索引，按大小与名称条件秒级定位大文件和大目录（目录已按支配度去重）。"
                Style="{StaticResource PageDescriptionStyle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <StackPanel Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <ComboBox
                        Header="类型"
                        SelectedIndex="{x:Bind ViewModel.KindIndex, Mode=TwoWay}"
                        MinWidth="130">
                        <ComboBoxItem Content="仅文件" />
                        <ComboBoxItem Content="仅目录" />
                        <ComboBoxItem Content="文件 + 目录" />
                    </ComboBox>
                    <TextBox
                        Text="{x:Bind ViewModel.MinSizeMb, Mode=TwoWay}"
                        Header="最小大小 (MB)"
                        Width="130" />
                    <TextBox
                        Text="{x:Bind ViewModel.NameFilter, Mode=TwoWay}"
                        Header="名称包含（如 .iso）"
                        Width="200" />
                    <Button
                        Command="{x:Bind ViewModel.SearchCommand}"
                        Style="{StaticResource AccentButtonStyle}"
                        VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE721;" FontSize="16" />
                            <TextBlock Text="搜索" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <TextBlock
                    Text="{x:Bind ViewModel.IndexStatus, Mode=OneWay}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                <StackPanel Spacing="6" Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}">
                    <ProgressBar Value="{x:Bind ViewModel.ProgressValue, Mode=OneWay}" Maximum="100" />
                    <TextBlock
                        Text="{x:Bind ViewModel.ProgressPhase, Mode=OneWay}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <TextBlock
            Grid.Row="2"
            Margin="1,12,0,8"
            Text="{x:Bind ViewModel.Status, Mode=OneWay}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <ListView
            Grid.Row="3"
            ItemsSource="{x:Bind ViewModel.Results}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:SearchResultRow">
                    <Grid ColumnSpacing="12" Padding="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="96" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Kind}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Size}" HorizontalAlignment="Right" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Path}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="3" Text="{x:Bind Detail}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
